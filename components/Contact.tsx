import React from 'react'

type CustomTextFieldProps = {
    label: string,
    name: string,
    changeHandler: (event: React.ChangeEvent<HTMLInputElement>) => void,
}

const Contact = (props: CustomTextFieldProps) => {
    return (
        <div></div>
    )
}

export default Contact